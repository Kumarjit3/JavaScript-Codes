console.log(Math.abs(-4)); // -4 to +4 but it can not be +4 to -4

console.log(Math.round(4.5));   //Always choose the heights between left and right of dot

console.log(Math.ceil(4.0));   //Always increase  the left value of the dot by +1 if the right value of the dot is bigger than zer(0) 

console.log(Math.floor(33.1)); //cut the right values after the dot

console.log(Math.min(3,4));  //choose the min value

console.log(Math.max(4,55));  //choose the max value

console.log(Math.sqrt(4));   //square root of a number

console.log(Math.random());   // generate random number between 0-1

console.log(Math.random()*10 + 1);

const min = 10;
const max = 20;

console.log( Math.floor(Math.random() * (max-min+1)) );  //generate random between min and max



